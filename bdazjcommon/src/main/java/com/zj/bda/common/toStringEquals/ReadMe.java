package com.zj.bda.common.toStringEquals;

/**
 * @author dongguabai
 * @Description
 * @Date 创建于 2019-05-13 14:58
 */
public class ReadMe {
    /**
     *
     @Override
     public boolean equals(Object obj) {
     if (this == obj) {
     return true;
     }
     if (!(obj instanceof Org)) {
     return false;
     }
     return !Objects.isNull(getId()) && getId().equals(((Org) obj).getId());
     }

     @Override
     public int hashCode() {
     return Objects.hash(getId());
     }

     */
}
